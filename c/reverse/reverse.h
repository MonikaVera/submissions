#ifndef REVERSE_H
#define REVERSE_H

int reverseThem(int, char **);
char** beolvas(int*, FILE*);
void reverse(char **, int);
void print(char **, int );
void calling(char **, int *);

#define INIT 4
#define SOR 1024

#endif